let number = 20; // Inicializar la variable con 20

while (number >= 1) { // Condición: mientras sea mayor o igual a 1, ese es el limite
  console.log(number); // Imprimir el valor actual
  number--; // Decrementar en 1
}