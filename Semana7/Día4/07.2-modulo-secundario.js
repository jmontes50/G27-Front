const esPar = (num) => {
    //verificamos si el número es par con base al residuo
    return num % 2 === 0;
}

//la exportación por defecto se utiliza si solo se va a exportar 01 sola cosa
export default esPar;